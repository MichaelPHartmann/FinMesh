version: 1.7.13

sphinx:
  configuration: docs/conf.py

outputs:
  - pdf

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
    - method: setuptools
      path: package

  
